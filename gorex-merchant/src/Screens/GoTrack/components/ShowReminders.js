import React, { memo } from "react";
import { View, Text } from "react-native";

function ShowReminders(props) {
  const {} = props;
  return (
    <View>
      <Text>Hello devices</Text>
    </View>
  );
}

export default memo(ShowReminders);
